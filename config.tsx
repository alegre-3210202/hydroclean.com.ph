import { Server } from "./schemas/Server";

export const server: Server = {
    url: "http://localhost",
    port: 80
}

export const emailServer: Server = {
    url: "http://localhost",
    port: 465
}